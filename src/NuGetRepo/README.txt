The only purpose of this project is to keep the track of 3rd party NuGet packages.
All files required to build the solution are manually copied from automatically 
restored "packages" folder into "lib" folder and referenced directly from there.